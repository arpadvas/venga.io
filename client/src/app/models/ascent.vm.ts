export interface AscentVM {
    id: string;
    name: string;
    type: string;
    grade: string;
    style: string;
    sentDate: string;
    location: {country: string, crag: string, sector: string};
}